import React from 'react';
import * as s from './Model.theme';

export const Model: React.FC = () => {
    return <>
        <p> Welcome to the 3d model page!</p>
        <s.ModelButton>I do nothing!</s.ModelButton>
    </>
}