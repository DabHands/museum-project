import styled from 'styled-components'

const Wrapper = styled.div`
    
`;

const Image = styled.img`
    max-width: 100%;
    height: auto;
    object-fit: cover;
`;


export {
    Wrapper,
    Image
};